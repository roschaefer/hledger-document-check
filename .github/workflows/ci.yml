name: CI

on:
  push:
  pull_request:

jobs:
  ci:
    uses: roschaefer/reusable-workflows/.github/workflows/rust-ci.yml@bb93bad5ab1cc573b8d0b24ef0b569efdc18d1b3 # main
    permissions:
      contents: write
    with:
      binary_name: hledger-document-check
      needs_hledger: true
      needs_fava: true
